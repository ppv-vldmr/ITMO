pi{SessionId}(
    Sessions
) except pi{SessionId} (
    pi{ContestId, SessionId, Letter}(
        Problems njoin Sessions
    ) except pi{ContestId, SessionId, Letter}(
        Runs njoin Sessions
    )
)