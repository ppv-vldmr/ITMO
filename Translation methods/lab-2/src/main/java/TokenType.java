public enum TokenType { //виды токенов, с которыми будем работать
    LEFT_PAREN, //(
    RIGHT_PAREN_SEM, //);
    TYPE,
    NAME,
    COMA, //,
    POINTER, //*
    END //символ окончания входных данных
}
